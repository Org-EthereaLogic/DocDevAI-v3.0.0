# Core Dependencies
click>=8.1.0
pydantic>=2.0.0
python-dotenv>=1.0.0
pyyaml>=6.0
psutil>=5.9.0

# Database
sqlalchemy>=2.0.0
alembic>=1.12.0
# Note: sqlcipher3 requires special installation, commented out for initial build
# sqlcipher3>=0.5.0

# Security
cryptography>=41.0.0
argon2-cffi>=21.3.0
bleach>=6.0.0  # HTML sanitization to prevent XSS
beautifulsoup4>=4.12.0  # HTML parsing as fallback for bleach

# Mathematical/Scientific Computing
numpy>=1.24.0  # Required for M003 MIAIR Engine
scikit-learn>=1.3.0  # For clustering and similarity in M009
sentence-transformers>=2.2.0  # For semantic similarity in M009 cache

# Token Optimization (M004 Pass 2)
tiktoken>=0.5.0  # OpenAI tokenizer for accurate token counting

# HTTP Client for M008 LLM Adapter
aiohttp>=3.8.0

# AI/ML Libraries (M008 LLM Adapter providers - install what you need)
# openai>=1.0.0  # For OpenAI provider
# anthropic>=0.7.0  # For Anthropic provider  
# google-generativeai>=0.3.0  # For Google provider

# Performance Optimization (M009 Pass 2)
mmh3>=3.0.0  # Fast MurmurHash3 for caching
lz4>=4.3.0  # Fast compression for cache
uvloop>=0.17.0  # Fast event loop for async operations
redis>=4.5.0  # Optional: distributed caching

# Development Tools
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-asyncio>=0.21.0  # For async tests
black>=23.0.0
pylint>=3.0.0
mypy>=1.5.0

# Documentation
jinja2>=3.1.0